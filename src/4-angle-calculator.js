export const angleCalculator = (turns) => {
  if (turns === 0) {
  return turns * 0;
  } else if (turns === 1) {
    return turns * 360;
  } else if (turns === 1.5) {
    return turns * 360;
  } else if (turns === 2) {
    return turns * 360;
  }
}
